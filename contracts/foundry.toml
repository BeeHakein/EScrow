[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
remappings = ["forge-std/=lib/forge-std/src/"]

# Base Sepolia (84532) is the target testnet for the chain adapters. RPC + keys are supplied
# via env at deploy time (never committed) — see the chain-adapter slice.
